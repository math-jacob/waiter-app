import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;
  max-width: 1216px;

  // margin-top == margin-bottom == 40
  // margin nas laterais auto --> portanto centralizará o container
  margin: 40px auto;

  display: flex;
  gap: 32px;
`
